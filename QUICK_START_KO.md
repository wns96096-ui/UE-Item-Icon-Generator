# 아이템 아이콘 생성기 — 팀원용 빠른 사용법

스태틱 메시를 선택해 UI용 `Texture2D` 아이콘을 한 번에 만드는 도구입니다.

![스태틱 메시를 선택해 아이콘 생성 목록에 추가하는 과정](Docs/Media/select-static-meshes.gif)

## 사용 순서

1. 콘텐츠 브라우저에서 아이콘으로 만들 **스태틱 메시를 모두 선택**합니다.
2. 상단 메뉴에서 **도구 > 아이템 아이콘 생성기**를 엽니다.
3. **콘텐츠 브라우저 선택 추가**를 누릅니다.
4. 목록에서 메시를 선택해 미리보기를 확인하고 구도를 조정합니다.
5. **전체 생성**을 누릅니다.

생성된 아이콘은 기본적으로 다음 폴더에 저장됩니다.

`/Game/Generated/ItemIcons`

## 미리보기 조작

| 조작 | 기능 |
| --- | --- |
| 마우스 왼쪽 드래그 | 메시 회전 |
| W / A / S / D | 화면 안에서 위치 조정 |
| 마우스 휠 | 확대 / 축소 |
| 새로고침 | 미리보기 즉시 다시 생성 |

> 이미지: 미리보기 조작 화면

## 자주 쓰는 설정

- **해상도**: 보통 512 권장
- **투명 배경**: UI 아이콘이면 켜기
- **이름 규칙**: 기본값 `T_Icon_{MeshName}` 사용
- **기존 에셋 덮어쓰기**: 이미 만든 아이콘을 갱신할 때만 켜기
- **이 메시만 별도 설정**: 특정 메시만 구도나 조명을 다르게 할 때 켜기

## 밝기 조절

- **키 조명**: 주 조명
- **필 조명**: 어두운 면 보완
- **백라이트**: 뒤쪽 윤곽 강조
- **스카이**: 전체적인 기본 밝기
- **노출**: 결과가 전체적으로 너무 밝거나 어두울 때 조정
- **채도**: `1.0`이 원본 색상

처음에는 조명색을 흰색으로 두고 **키/필/백 강도와 노출만 조정**하면 됩니다.

## 한 개만 저장할 때

목록에서 메시를 선택하고 구도를 맞춘 다음 **선택 미리보기 저장**을 누릅니다.

## 문제가 생기면

- **회전할 때 메시가 크게 움직임**: 메시 피벗이 중심에서 벗어난 경우입니다. WASD로 구도를 맞춥니다.
- **회색이나 흰색으로 보임**: 테스트 머티리얼의 원래 색상, 머티리얼 슬롯, 조명과 노출을 먼저 확인합니다. 처음 불러온 에셋이라면 셰이더 컴파일 완료 후 **새로고침**도 시도합니다.
- **저장 실패**: 출력 폴더가 `/Game`으로 시작하는지 확인합니다.
- **같은 이름이 있다고 실패**: **기존 에셋 덮어쓰기**를 켭니다.
- **일부만 다시 만들기**: 만들 항목만 `사용` 체크 후 **전체 생성**을 누릅니다.

![투명 배경으로 생성된 Texture2D 아이콘](Docs/Media/generated-transparent-icon.gif)

## 팀 공통 권장값

```text
출력 폴더: /Game/Generated/ItemIcons
이름 규칙: T_Icon_{MeshName}
해상도: 512
투명 배경: 켜기
기존 에셋 덮어쓰기: 평소에는 끄기
```

상세한 기능 설명은 `README_KO.md`를 참고하세요.
